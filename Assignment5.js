// Q1:
// Explain difference between innerText and innerHTML in the following example?
let content = document.getElementById('content');
console.log(content.innerHTML); // returns an elem with the tags
console.log(content.innerText); // returns only the text within the tag